#ifndef STRINGS_OPS

#define STRINGS_OPS

class reverse
{
	   public:
			 void str_reverse(std::string& s);
			 void reverse_by_k (std::string& s, int k);
			 void reverse_vowels (std::string& s);
			 void reverse_words (std::string& s);
};

#endif
